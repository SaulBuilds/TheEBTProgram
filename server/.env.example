# Server env
PORT=8080
GCP_BUCKET=ebt_profilepics
# If using a service account file, point to it (or rely on ADC in CI/cloud)
GCP_KEYFILE_PATH=/path/to/keyfile.json
# Allowed origin for CORS (if you update server.js to enforce)
# CORS_ORIGIN=http://localhost:3000
